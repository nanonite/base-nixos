# ──────────────────────────────────────────────────────────────────────────────
# THIS FILE IS GENERATED DURING INSTALLATION — do not write it by hand.
#
# For Raspberry Pi, generate on the Pi itself after first boot, or cross-compile:
#   nixos-generate-config --root /mnt --show-hardware-config
#
# Typical RPi storage layout (SD card or NVMe via USB adapter):
#   /boot   → FAT32 (Pi bootloader partition)
#   /       → ext4 or btrfs root
#
# Note: RPi uses U-Boot / RPi firmware bootloader, NOT systemd-boot or GRUB.
# The boot.loader.raspberryPi settings in rpi4.nix handle this.
# ──────────────────────────────────────────────────────────────────────────────

# Paste the generated hardware-configuration.nix content below this line:

{ config, lib, pkgs, modulesPath, ... }:

{
  # REPLACE THIS ENTIRE FILE with the output of:
  # nixos-generate-config --root /mnt --show-hardware-config
}
